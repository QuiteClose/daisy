# Feedback Archive

Entries already folded into a prompt's `## Rules` section by `daisy optimize`. Kept for historical reference; not read by any script. Entries move here automatically from `feedback.md` when a `daisy optimize` run uses them.

---
