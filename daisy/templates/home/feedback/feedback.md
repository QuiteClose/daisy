# Feedback Log

Captures cases where Daisy did the wrong thing. Used by `daisy optimize` to improve prompts.

Each entry should describe what happened, what should have happened, and why the prompt failed.
Run `daisy optimize` once you have several entries to generate an improved prompt.

---
