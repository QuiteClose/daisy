.daisy

# Environment and secrets
.env.sh

# OS generated files
.DS_Store
Thumbs.db

# Healthcheck cache stamp — rewritten on every passing run, so tracking it
# would sweep a meaningless timestamp diff into every home/ auto-commit.
home/*/.healthcheck.ttl

# Daisy workspace config (per-machine)
.daisy/
.cursor/rules/daisy.mdc
.cursor/rules/daisy-logging.mdc
.cursor/rules/daisy-plan.mdc
.claude/commands/daisy.md
.claude/settings.local.json
PLAN.md
